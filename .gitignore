# graphify's generated knowledge graph — rebuilt with `graphify update .`
graphify-out/

# Downloaded third-party source material for the research dossier.
# 19 arXiv papers as text + four cloned reference repos bundled to single files
# + MuJoCo docs pages. Not committed: it is other people's copyrighted text and
# code, and it is fully regenerable — see research/tools/ and research/README.md.
research/sources/

# Python / uv
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Secrets and local machine config
.env
.env.*
*.pem
*.key
.claude/settings.local.json

# Training output: checkpoints, tensorboard, wandb cache, rendered rollouts
logs/
wandb/
*.pt
*.mp4

# MuJoCo appends a warning log on every run
MUJOCO_LOG.TXT

# OS junk
.DS_Store
